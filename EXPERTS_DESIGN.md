# 多专家分析 Agent 设计文档

> 版本：v1.2　日期：2026-09-14　状态：**已实现并验证通过**
>
> 修订记录：
> - v1.1 — 确认专家配置（含分类体系改造）、风险官措辞强度可配置、单轮辩论、权重方案、分歧度阈值
> - v1.2 — **实现完成**。修正 §2/§5 的自相矛盾（风险官由"并行"改为"分析师之后串行执行"），补充实测耗时与验证结果
> 目标：把「分析研判 Agent」从单次 LLM 生成，升级为**多专家协作 + 风险官质疑 + 首席汇总**的架构

---

## 1. 背景与动机

### 1.1 现状问题

当前研判流程（`app/agent/graph.py`）是**单次 LLM 生成**：

```
组装数据（每类新闻取6条 + 行情快照） → 一个 prompt → 一份报告
```

存在三个缺陷：

| 缺陷 | 表现 |
|------|------|
| **视角单一** | 一个 prompt 里塞所有维度，LLM 只能"泛泛而谈"，缺少专业纵深 |
| **乐观偏差** | LLM 天然倾向给正面结论，缺少对抗性视角，风险提示流于形式 |
| **无法表达分歧** | 所有判断混在一个输出里，无法体现"宏观面看多但资金面看空"这类真实矛盾 |

### 1.2 解决思路

金融分析**天然是多视角的**——同一天的消息，宏观分析师看流动性、行业分析师看景气度、资金面看筹码，结论可能相反。**多专家架构正好匹配这个特性**。

核心设计：**4 位领域分析师独立分析 + 1 位风险官专门唱反调 + 1 位首席策略师汇总裁决**。

---

## 2. 总体架构

```
                    今日数据（已分类新闻 + 行情 + 资金/宏观数据）
                                  │
                    ┌───────────────▼───────────────┐
                    │        prepare（数据准备）       │
                    │  分类分发 + 综合类广播 + 算技术指标  │
                    └───────────────┬───────────────┘
                                    │  并行 fan-out
        ┌──────────┬────────────────┼────────────────┬──────────┐
        ▼          ▼                ▼                ▼
   ┌────────┐ ┌────────┐     ┌──────────┐     ┌────────┐
   │宏观分析师│ │行业分析师│     │资金面分析师│     │技术面分析师│
   └────┬───┘ └────┬───┘     └─────┬────┘     └────┬───┘
        └──────────┴────────────────┼───────────────┘
                                    │  fan-in（4 份独立观点）
                          ┌─────────▼─────────┐
                          │  风险官（读 4 份观点）│
                          │ 逐条反驳 + 定风险等级 │
                          └─────────┬─────────┘
                                    │
                          ┌─────────▼─────────┐
                          │ aggregate（代码定档）│
                          │ 加权分/分歧度/置信度 │
                          └─────────┬─────────┘
                                    │
                          ┌─────────▼─────────┐
                          │   首席策略师（撰写）  │
                          └─────────┬─────────┘
                                    ▼
                             最终研判报告（入库）
```

### 2.1 关键设计原则

1. **专家的独立性**：4 位分析师**互相看不到对方结论**，各自基于自己的数据维度独立判断。这样才能产生真实分歧。
2. **风险官的特殊性**：唯一能看到其他专家结论的角色，职责是**质疑和反驳**，不参与方向平均。
3. **首席的裁决权**：负责综合多数意见，但**必须认真对待风险官的反驳**，分歧大时下调置信度。
4. **两阶段执行** ⭐：4 位分析师**并行**跑（总耗时≈最慢的一个）；**风险官在其后串行执行**——
   因为它的核心价值是「逐条反驳具体观点」，**必须先看到分析师的结论**。
   （注：这与 §4.5「风险官输入含其他专家结论」一致；早期草稿曾把风险官画进并行批次，属自相矛盾，已修正。）
   随后 `aggregate`（代码，无 LLM）与 首席（LLM）依次执行。
5. **`综合` 类新闻广播**：无法归入特定类别的新闻（突发事件、跨类别）**分发给全部专家**，而非只给某一位。理由见 §4.0。

> **前置依赖**：本设计依赖「数据采集 Agent」的分类体系改造（新增 `市场`/`综合` 两类、
> 修正兜底逻辑、给 LLM 诚实的"无法归类"出口）。详见 `AGENTS_DESIGN.md` §2.4。
> **若不先修分类，`行业` 会继续充当垃圾桶，行业分析师的判断仍会被污染。**

---

## 3. 数据契约（统一输出格式）

专家输出必须结构化，才能被首席汇总。定义两个 schema：

### 3.1 分析师输出 `ExpertOpinion`（4 位分析师共用）

```python
class ExpertOpinion(BaseModel):
    expert: str                      # 专家名：宏观/行业/资金面/技术面
    stance: Literal["看多", "中性", "看空"]
    score: float                     # -1(极空) ~ 1(极多)
    confidence: Literal["high", "medium", "low"]
    key_points: list[str]            # 核心论据（每条须附依据）
    evidence: list[str]              # 引用的新闻标题 / 数据点
    uncertainties: list[str]         # 本维度看不清楚的地方
```

#### 3.1.1 `score` 打分锚点（四位分析师统一）⭐

`score` 会进入**加权平均**（§6.3）与**分歧度**（§6.2）——若四人的刻度不一致，
这两个指标同时失真：加权平均等于把不同尺子量出的数相加，分歧度则退化成"谁习惯打大数"。

因此 prompt 中**必须**给出统一刻度（不得只写"范围 -1~1"）：

| 区间 | 档位 | 含义 |
|---|---|---|
| `+0.5 ~ +1.0` | 强多 | 多重、独立、可验证的利好叠加 |
| `+0.15 ~ +0.5` | 偏多 | 有明确积极因素，但存在明显不确定性 |
| `-0.15 ~ +0.15` | 中性 | 信号混杂、多空相抵，或信息不足 |
| `-0.5 ~ -0.15` | 偏空 | 有明确负面因素 |
| `-1.0 ~ -0.5` | 强空 | 多重利空叠加 |

约束：

1. **中性带 `±0.15` 与 `aggregate_node` 的定调阈值对齐**，不得各写各的
2. 输出须含 `score_band` / `score_reason` 两个字段（强制自检；
   `ExpertOpinion` 不持久化它们，仅用于让模型显式落到某一档）
3. **对称性要求**：锚点是对称的——N 条独立、可验证的利好与 N 条同等强度的利空，
   评分绝对值应当相当。若确在看多方向更保守，须在 `uncertainties` 说明理由
4. **禁止讨好式中性**：信息不足可以取中性档，但须说明缺什么；
   倾向明确时**不得**往零附近缩

> 为什么这四条这么定（含 LLM 极性偏差与杠杆效应的文献辨析）：
> 见本地决策记录 `打分锚点决策记录.md`（不上传）。

### 3.2 风险官输出 `RiskOpinion`

```python
class RiskOpinion(BaseModel):
    risk_level: Literal["高", "中", "低"]   # 整体风险等级
    risks: list[str]                        # 风险点清单（每条附依据）
    counter_arguments: list[str]            # 对分析师乐观结论的逐条反驳
    worst_case: str                         # 最坏情况推演
    blind_spots: list[str]                  # 当前分析可能忽略的盲区
```

### 3.3 首席输出 `FinalReport`（兼容现有 `MarketReport`）

```python
class FinalReport(BaseModel):
    # —— 以下字段沿用现有 MarketReport，保证前端兼容 ——
    market_summary: str
    sentiment: Literal["偏多", "中性", "偏空"]
    confidence: Literal["high", "medium", "low"]
    score: float
    key_drivers: list[str]
    sector_opportunities: list[str]
    risks: list[str]
    reference_news: list[str]
    # —— 新增字段 ——
    expert_opinions: list[dict]      # 5 位专家的原始观点（用于前端展示）
    divergence: float                # 分歧度（0~2，越大分歧越大）
    consensus_note: str              # 对分歧的说明
    risk_veto: bool                  # 风险官是否触发了置信度下调
```

---

## 4. 各专家 Agent 详细设计

### 4.0 专家输入的分发规则

| 输入类型 | 分发给谁 |
|---------|---------|
| `宏观` / `政策` 类新闻 | 宏观分析师 |
| `行业` / `公司` 类新闻 | 行业分析师 |
| `资金` 类新闻 | 资金面分析师 |
| 行情 K 线数据 | 技术面分析师 |
| **`综合` 类新闻** | **全部 5 位专家（广播）** ⭐ |
| **全部新闻** | 风险官（本就全看） |
| 首席汇总所需 | 5 位专家结论 + 行情 + `综合` 类新闻 |

**为什么 `综合` 类要广播而不是只给风险官**：

1. `综合` 的定义就是"跨类别"，那自然该被多个视角看到。例如"地震致工厂停产"——
   行业分析师要看（供给冲击）、宏观分析师也要看（经济影响），只给一个人是**信息损失**。
2. **避免系统性偏空**：若只有风险官能看到综合类新闻，其中的**利好信息**会被单方面做负面解读，
   导致研判整体偏向悲观。
3. **零额外成本**：广播不增加 LLM 调用次数（各专家本来就要跑）。

> 备选方案：若后续发现 `综合` 类新闻量大（占比 >10%）且内容重要，可升级为**新增第 6 位
> 「综合/事件分析师」**专责处理。**先按广播实现，跑一轮看数据再定。**

### 4.1 宏观分析师

| 项 | 内容 |
|----|------|
| **职责** | 判断货币/财政政策与外部环境对 A 股流动性的影响 |
| **输入** | `宏观`+`政策` 类新闻（近 1 天）、宏观数据表（CPI/PMI/LPR，待接入）、美股行情 |
| **关注点** | 降准降息、逆回购、LPR、财政投放、美联储、汇率、GDP/CPI/PMI |
| **工具** | `get_macro_data`（待实现）、`get_market_overview` |
| **输出** | `ExpertOpinion`（expert="宏观"） |

**Prompt 要点**：要求区分"政策信号"与"政策落地"，避免把表态当利好；必须引用具体新闻。

### 4.2 行业分析师

| 项 | 内容 |
|----|------|
| **职责** | 识别板块轮动方向与具体机会 |
| **输入** | `行业`+`公司` 类新闻、题材标签（`themes`）、**板块实际表现**（已接入：`sector_data` 表，2026-09-16） |
| **关注点** | 产业链涨价/降价、政策扶持、龙头动向、题材热点聚类 |
| **工具** | `get_recent_news(category="行业")`（板块数据已由 `prepare_node` 直接注入输入，无需工具） |
| **输出** | `ExpertOpinion`（expert="行业"） |

**Prompt 要点**：必须给出**具体的板块名称**，禁止"科技板块"这类泛化表述；用 `themes` 标签做热点聚类。

### 4.3 资金面分析师

| 项 | 内容 |
|----|------|
| **职责** | 判断资金态度（增量入场 or 存量博弈 or 撤离） |
| **输入** | `资金` 类新闻、北向资金/两融数据（待接入）、行情成交量 |
| **关注点** | 北向净流入/流出、融资余额、主力资金、量能变化、ETF 申赎 |
| **工具** | `get_fund_flow`（待实现）、`get_market_overview` |
| **输出** | `ExpertOpinion`（expert="资金面"） |

**Prompt 要点**：**资金数据缺失时须诚实标注**，不得凭空推断（这是最容易幻觉的维度）。

### 4.4 技术面分析师

| 项 | 内容 |
|----|------|
| **职责** | 从价格/量能形态判断短期多空 |
| **输入** | 指数近 30 天 K 线（**已有数据，无需新采集**） |
| **关注点** | MA5/MA20/MA60 多空排列、量能变化、涨跌动量、RSI、支撑/压力位 |
| **工具** | 纯计算（pandas 算指标），LLM 只负责解读 |
| **输出** | `ExpertOpinion`（expert="技术面"） |

**Prompt 要点**：**指标由代码算好再喂给 LLM**（不让 LLM 自己算，避免算错）；要求明确指出"站上/跌破哪条均线"这种可验证的表述。

> 技术指标计算：MA、量比、动量、RSI 用 pandas 实现，放 `app/agent/indicators.py`。

### 4.5 风险官（关键角色）

| 项 | 内容 |
|----|------|
| **职责** | 系统性找风险 + **反驳其他专家的乐观结论** |
| **输入** | 全部新闻 + **其他 4 位专家的结论**（唯一能看到他人观点的角色） |
| **关注点** | 被忽略的利空、估值风险、机构减仓、外部冲击、逻辑漏洞 |
| **输出** | `RiskOpinion` |

**Prompt 要点**（最重要）：
- 明确角色设定：「你是一位**专职唱反调**的风险官，你的价值在于找出其他人忽略的风险，而不是附和」
- 要求**逐条反驳**：「针对宏观分析师的『流动性宽松』结论，指出其中的漏洞」
- 要求给出**最坏情况**推演
- **禁止无依据的**表述：「风险可控」要给依据；**若确实未发现明确利空，应如实给"低"并说明**。
  把"低"也说成"高"，会让降级机制失去区分度 —— 那是失职而不是尽责。
  （原文曾是「**禁止**输出"风险可控""整体乐观"这类无效表述」，
  字面封死了"低"这个出口，与 §4.5.1 的「等级由事实决定」相违背，已改。）
- 语气由 **`RISK_INTENSITY` 参数**控制（见 §4.5.1）

#### `risk_level` 三档锚点（2026-09-21 补，与 §4.5.1 同源）

评的是**形势本身有多危险**，与**分析师有多乐观**无关 ——
反驳得多 ≠ 风险高：分析师集体看多时你要挑的刺自然多，
那说明的是你的工作量，不是市场的危险程度。

| 等级 | 判定标准 |
|---|---|
| **高** | 存在**可验证的**、可能造成显著回撤的利空（政策转向、资金持续净流出、技术破位、外部冲击） |
| **中** | 有值得警惕的风险点，但影响可控，或已有对冲 |
| **低** | 未发现明确利空。**日常波动、个股减持、增速小幅回落、估值偏高等常态噪音，属于「无明确利空」，不构成「中」或「高」** |

> 为什么必须有这把尺子：改动前实测过一个**全是利好**的场景
> （降准 1 万亿 + 北向净流入 200 亿 + 指数涨 2.5%），风险官连测 3 次都判**「高」**，
> 而「低」在 5 次探测里从未出现 —— 三级刻度实际退化成了两级。
> 根因是「逐条反驳」这个职责让等级被分析师的乐观程度驱动。

> 这个角色是对抗 LLM「一味看多」倾向的核心机制，是本次升级最有价值的部分。

#### 4.5.1 措辞强度机制（可配置）

风险官的"尖锐程度"做成**可调参数**，不写死在 prompt 里。

**参数定义**：

| 项 | 值 |
|----|-----|
| 配置项 | `RISK_INTENSITY`（`app/config.py`，可由环境变量覆盖） |
| 取值范围 | `[0.0, 1.0]` |
| 中性点 | `0.5` |
| **方向** | **数值越小越尖锐**（`0.4` = 倾向尖锐） |
| 当前值 | **`0.4`** |

**分档与语气映射**：

| 区间 | 档位 | 语气指令（注入 prompt） |
|------|------|----------------------|
| `[0.00, 0.30)` | 极度尖锐 | 扮演坚定的空头，主动质疑一切乐观结论，不留情面指出逻辑漏洞 |
| `[0.30, 0.45)` | **倾向尖锐** ← 当前 0.4 | 主动寻找逻辑漏洞，不轻易接受乐观结论，对每条多头观点提出质疑 |
| `[0.45, 0.55]` | 中性 | 客观陈述风险，不预设立场 |
| `(0.55, 0.70]` | 倾向温和 | 提示风险，但认可合理的乐观论据 |
| `(0.70, 1.00]` | 温和 | 仅做常规风险提示，措辞克制 |

**实现方式**：`RISK_INTENSITY` 决定注入哪一段「语气指令」文本（查表），
与风险官的 prompt 模板拼接。**改配置即改语气，无需改代码。**

> 强度**不影响** `risk_level` 的判定逻辑（仍由事实决定），只影响**措辞的尖锐程度**。
> 这样避免"调高尖锐度就把风险等级也抬上去"的耦合。

> ⚠️ 待实现时确认：`0.4` 落在"倾向尖锐"档，即**数值越小越尖锐**。若后续希望方向反过来
> （数值越大越尖锐），只需调整映射表，不影响其他逻辑。

### 4.6 首席策略师

| 项 | 内容 |
|----|------|
| **职责** | 汇总 5 位专家观点，处理分歧，输出最终研判 |
| **输入** | 5 位专家输出 + 行情快照 + 分歧度（代码算好） |
| **输出** | `FinalReport`（兼容现有 `MarketReport`） |

**汇总规则**（代码 + LLM 混合）：

1. **最终 score** = Σ(分析师 score × 权重) ÷ **全量权重和**（权重见 §6.1），风险官**不参与**方向平均。
   分母固定为全量权重而非「在场专家权重和」——缺席专家贡献 0，避免权重被静默摊给其他人。
2. **分歧度** = max(专家score) − min(专家score)，由代码计算
3. **置信度调整**（代码执行）：
   - 风险官 `risk_level="高"` → 置信度**降一档**，`risk_veto=True`
   - 分歧度 > 1.0 → 置信度**再降一档**
   - **有专家缺席（分析失败）→ 再降一档**（信息不完整）
4. **LLM 职责**：基于上述数值，撰写 `market_summary`、整合 `key_drivers`、`sector_opportunities`、`consensus_note`
5. **强制要求**：`risks` 字段必须**包含风险官的所有风险点**（不得过滤删减）

---

## 5. 执行流程与时序

```
① prepare 节点
   ├─ 从 DB 读近 1 天新闻，按 category 分组
   ├─ 提取「综合」类新闻，作为公共输入准备广播给全部专家
   ├─ 读行情快照（指数近 30 天）
   ├─ 计算技术指标（MA/RSI/量比/动量）
   └─ 计算北向/两融等（若已有数据）

② 4 位分析师**并行** fan-out（互不可见，保证观点独立）
   ├─ 宏观分析师   ┐
   ├─ 行业分析师   │
   ├─ 资金面分析师  ├─ 并行
   └─ 技术面分析师  ┘

③ 风险官（**串行**，必须看到 ② 的结论）
   └─ 逐条反驳 + 定风险等级 + 最坏情况

④ aggregate（**代码定档，无 LLM**）
   ├─ 加权 score（仅 4 位分析师，风险官不参与；分母为**全量权重和**）
   ├─ 分歧度 = max − min
   └─ 置信度调整（分歧度 ＞1.0 / 风险官判「高」/ **有专家缺席** → 各降一档）

⑤ 首席策略师（LLM 撰写）
   └─ 综合观点 + 体现分歧 + **强制并入风险官全部风险点**

⑥ 入库
   └─ 写入 report 表（含 expert_opinions / divergence / risk_veto）
```

**耗时实测**（2026-09-14）：4 位分析师并行约 **5 秒** 完成；风险官（输入 40 条新闻 + 4 份观点）约 **11 秒**；
首席撰写约 **10 秒**。**总计约 30 秒**，可接受。

---

## 6. 关键算法

### 6.1 专家权重

| 专家 | 权重 | 定位 |
|------|------|------|
| 宏观 | **0.30** | 主导变量（最高） |
| 行业 | **0.25** | 中观方向 |
| 资金面 | **0.25** | 中观约束 |
| 技术面 | **0.20** | 短期辅助（最低） |
| **风险官** | **不参与** | 仅用于下调置信度 |

#### 权重设定理由（逐条）

**宏观 = 0.30（最高）**
A 股是典型的**政策市**——货币政策（降准降息、逆回购）、财政投放、监管基调是决定市场整体方向的
**主导变量**，且影响面覆盖全市场所有板块。相比其他维度，它作用于"水位"而非"某条船"，
是 β 的来源，所以权重最高。

**行业 = 0.25**
直接指向**可操作的机会**（哪个板块有机会），信息价值高。但单个行业的影响面窄于宏观
（只影响局部），且行业消息常有噪音（概念炒作、一日游行情），因此低于宏观、与资金面并列。

**资金面 = 0.25**
短期走势的**硬约束**——没有增量资金入场，再好的利好也涨不动；反之资金持续流出时，
基本面再好也难有表现。它和行业并列，是因为二者构成"中观"的两个侧面：
**行业回答"涨什么"，资金回答"涨不涨得动"**。

**技术面 = 0.20（最低）**
两个原因：
1. **无独立信息源** —— 技术指标是价格的再加工，不含新增基本面信息，本质是对其他维度结果的验证；
2. **易失效** —— 假突破、指标钝化在 A 股很常见，作为独立信号可靠性偏低。

因此它定位为**短期择时辅助**，权重最低。

**风险官不参与方向加权**
风险官输出的不是"看多/看空"的方向判断，而是**风险约束**。若让它参与加权，会造成
**双重计数**——它的判断同样来自那批新闻，等于同一信息被计两次，且方向相反，会系统性
压低 final_score。正确用法是**只通过它调整置信度**（`risk_level="高"` → 降一档）。

> 权重为初始设定，**后续可根据回测结果调优**（哪种视角预测更准，就给更高权重）。
> 权重将作为配置项实现，不写死。

### 6.2 分歧度

```
divergence = max(分析师 score) − min(分析师 score)     # 范围 0~2
```

| 分歧度 | 含义 | 处理 |
|--------|------|------|
| < 0.5 | 高度一致 | 置信度可上调一档 |
| 0.5 ~ 1.0 | 存在分歧 | 维持 |
| > 1.0 | 严重分歧 | 置信度降一档，`consensus_note` 说明分歧点 |

### 6.3 最终 score

```
final_score = Σ(专家 score × 权重)     # 仅4位分析师
```

映射到 `sentiment`：`> 0.15` 偏多｜`< −0.15` 偏空｜其余中性。

---

## 7. 技术实现

### 7.1 编排框架：LangGraph

用 **LangGraph** 实现 fan-out / fan-in（项目已依赖 `langgraph`）。

```
State:
  market_snapshot: str
  news_by_category: dict[str, list]
  indicators: dict
  opinions: list[ExpertOpinion]      # 累积
  risk_opinion: RiskOpinion
  divergence: float
  final_report: FinalReport

图结构:
  START → prepare → [macro, industry, capital, technical, risk] → aggregate → chief → END
```

> 这是项目**第一次真正使用 LangGraph**——之前 `graph.py` 名字叫 graph，实际是普通函数链。本次改造后，"多 Agent"才名副其实。

### 7.2 文件结构变更

| 文件 | 变更 |
|------|------|
| `app/agent/experts.py` | **新增**：5 位专家的 prompt 定义 + 调用函数 |
| `app/agent/indicators.py` | **新增**：技术指标计算（MA/RSI/量比/动量） |
| `app/agent/schemas.py` | **新增**：`ExpertOpinion` / `RiskOpinion` / `FinalReport` |
| `app/agent/graph.py` | **改造**：用 LangGraph 重写研判编排 |
| `app/agent/data_agent.py` | **变更**（前置）：分类体系改造——新增 `市场`/`综合`、修正兜底、prompt 加"无法归类"出口 |
| `app/config.py` | **变更**：新增 `RISK_INTENSITY`（默认 0.4）、专家权重配置 |
| `app/models/report.py` | **变更**：新增 `expert_opinions`(Text/JSON)、`divergence`(Float)、`risk_veto`(Boolean) |
| `app/services/report_service.py` | 微调：`report_to_dict` 透传新字段 |

### 7.3 数据库变更（report 表）

| 新字段 | 类型 | 说明 |
|--------|------|------|
| `expert_opinions` | Text (JSON) | 5 位专家原始观点 |
| `divergence` | Float | 分歧度 |
| `risk_veto` | Boolean | 风险官是否触发置信度下调 |
| `low_info` | Boolean | 当日**三**信号均未触发（市场平静） |
| `data_stale` | Boolean | 近期新闻不足、基于陈旧数据生成 |

> 沿用现有 `_migrate()` 轻量迁移机制自动补列。

---

## 8. 降级与容错

| 场景 | 处理 |
|------|------|
| 单个专家 LLM 调用失败 | 跳过该专家，用剩余专家汇总；报告中标注「XX 专家分析失败」 |
| 单个专家输出 JSON 解析失败 | 重试 1 次；仍失败则跳过 |
| **全部专家失败** | **降级到现有的单次生成模式**（保证每天都有报告） |
| 专家超时 | 单节点超时 60 秒，超时即跳过 |
| 资金/宏观数据缺失 | 对应专家如实标注"数据缺失"，**禁止幻觉填补** |

---

## 9. 成本估算

| 环节 | 改造前 | 改造后 |
|------|--------|--------|
| 情绪打分（批量） | ~10 次 | ~10 次（不变） |
| 研判生成 | 1 次 | **6 次**（5 专家 + 1 首席） |
| 每日合计 | ~50 次 | **~60 次** |

> ⚠️ **2026-09-22**：情绪打分环节**已整体删除**，上表「情绪打分」一行不再发生，
> 每日实际调用数比表中少约 10 次。表格保留作为当年的改造对比快照。

**增量成本极小**（每天多 5 次调用，约几分钱），但研判质量是量级提升。

---

## 10. 前端展示（每日研判页）

新增两块内容：

**① 专家观点卡片**（5 张并排）

```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 宏观分析师     │ │ 行业分析师     │ │ 资金面分析师   │
│ 🟥 看多 +0.5  │ │ 🟥 看多 +0.3  │ │ ⬜ 中性 +0.1  │
│ 置信度: 中     │ │ 置信度: 高     │ │ 置信度: 低     │
│ · 逆回购投放…  │ │ · AI算力政策…  │ │ · 北向小幅…   │
└──────────────┘ └──────────────┘ └──────────────┘

┌──────────────┐ ┌────────────────────────────────┐
│ 技术面分析师   │ │ ⚠️ 风险官（红色警示风格）        │
│ 🟩 看空 -0.2  │ │ 风险等级: 高                    │
│ 置信度: 中     │ │ · 估值已处高位，机构在减仓…      │
│ · 跌破20日线…  │ │ · 对"流动性宽松"的反驳：…        │
└──────────────┘ └────────────────────────────────┘
```

**② 分歧度指示器**

```
专家分歧度：0.7（存在分歧）
最终研判：偏多 · 置信度：中（风险官下调）
```

---

## 11. 实施步骤

| 阶段 | 内容 | 产出 |
|------|------|------|
| **Phase 1** | 定义 schema + 5 位专家 prompt | `schemas.py`、`experts.py` |
| **Phase 2** | 技术指标计算 | `indicators.py` |
| **Phase 3** | LangGraph 编排改造 | `graph.py` 重写 |
| **Phase 4** | 表结构 + 服务层兼容 | `report.py`、`report_service.py` |
| **Phase 5** | 前端专家卡片展示 | `streamlit_app.py` |
| **Phase 6** | 效果验证与调优 | 对比新旧报告质量 |

> Phase 1~4 完成即可跑通（后端），Phase 5 是展示层。

---

## 12. 可选增强（Phase 7，暂不做）

### 12.1 专家辩论轮次

现在是**单轮**：各专家独立出结论 → 首席汇总。

可升级为**双轮辩论**：
- 第一轮：各专家独立出结论
- 第二轮：各专家**看到其他人的结论**，进行反驳或修正
- 首席汇总

代价：LLM 调用从 6 次 → 11 次（仍便宜），耗时翻倍。
收益：结论更审慎，能体现"观点交锋"。

**建议**：先做单轮，效果满意后再考虑双轮。

### 12.2 专家权重自适应

根据回测结果，动态调整 4 位分析师的权重（哪种视角预测更准，就给更高权重）。依赖回测体系完善后再做。

---

## 13. 已确认的决策（2026-09-14）

| # | 决策项 | 结论 |
|---|--------|------|
| 1 | **专家配置** | 4 位分析师 + 1 风险官。**前置改造分类体系**（新增 `市场`/`综合`，修正兜底，prompt 给"无法归类"出口）——详见 `AGENTS_DESIGN.md` §2.4 |
| 2 | **`综合` 类新闻处理** | **广播给全部专家**（方案 A）。跑一轮看数据，若占比 >10% 且重要，再考虑升级为第 6 位「综合/事件分析师」 |
| 3 | **风险官措辞强度** | `RISK_INTENSITY = 0.4`（**数值越小越尖锐**），做成配置项，不写死。分档映射见 §4.5.1 |
| 4 | **辩论轮次** | **单轮**（各专家独立出结论 → 首席汇总）。双轮辩论留作 Phase 7 可选增强 |
| 5 | **专家权重** | 宏观 0.30 / 行业 0.25 / 资金面 0.25 / 技术面 0.20；风险官不参与。**理由见 §6.1**，权重做成配置项 |
| 6 | **分歧度阈值** | 0.5 / 1.0 两档（<0.5 一致 / 0.5~1.0 分歧 / >1.0 严重分歧） |
| 7 | **前端展示** | 按 §10 推进：专家观点卡片 + 分歧度指示器，放在「每日研判」页 |

### 13.1 待后续讨论

| # | 议题 | 状态 |
|---|------|------|
| 8 | **风险官与首席的产出是否需要验证** | 待讨论。核心问题：如何防止风险官"过尖"或"失声"、如何校验首席的汇总是否忠实反映了专家意见（可能引入 Reflection 机制） |

---

## 14. 与现有系统的一致性

- **兼容性**：`FinalReport` 保留现有 `MarketReport` 的全部字段，前端旧展示不受影响，新增字段为增量。
- **调用方式不变**：`report_service.run_daily_pipeline()` 对外接口不变，内部编排替换。
- **数据流向不变**：仍读取「数据采集 Agent」产出的干净新闻，仍写入 `report` 表。
- **降级保障**：全部专家失败时回落到现有单次生成，**不会出现"今天没有报告"的情况**。
