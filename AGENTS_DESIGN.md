# 多 Agent 架构设计文档（三 Agent 方案）

> 版本：v1.0　日期：2026-09-06　状态：待确认，未开始实现

---

## 1. 总体架构

三个专职 Agent 通过**数据流**串成一条主线，辅以定时任务编排：

```
                新闻源(新浪/东财/财联社)
                        │
                        ▼
        ┌───────────────────────────────┐
        │  ① 数据采集 Agent（Data Agent）  │  采集 + 相关性过滤 + 语义去重 + 多源合并 + 分类
        └───────────────┬───────────────┘
                        │ 干净、去重、带分类的金融新闻
                        ▼
        ┌───────────────────────────────┐
        │ ② 分析研判 Agent（Analyst Agent）│  情绪打分 + 多维分析 + 生成研判报告
        └───────────────┬───────────────┘
                        │ 结构化研判报告(入库)
                        ▼
        ┌───────────────────────────────┐
        │   ③ 问答 Agent（QA Agent）       │  检索 + 工具调用 + 交互回答（独立于主流程，按需触发）
        └───────────────────────────────┘
```

- **①② 是串行的主流程**，由定时任务驱动（每天自动跑）。
- **③ 是独立服务**，用户提问时才触发，读取①②沉淀的数据。
- 三者不共享状态，通过**数据库 + 向量库**交接，边界清晰、可独立测试。

> 设计原则：不追求"纯 LLM 自主循环"。数据采集 Agent 是「LLM 智能判断 + 向量计算」的混合体——LLM 只做它擅长的语义判断，向量/规则做它擅长的快速比对。这样成本可控、结果可复现。

---

## 2. ① 数据采集 Agent（Data Agent）—— 核心新增

### 2.1 职责

一句话：**把「原始快讯」变成「干净、去重、带分类的金融新闻」，是入库前的质检关卡。**

### 2.2 输入 / 输出

| | 内容 |
|---|---|
| 输入 | 原始 `NewsItem` 列表（title / content / source / url / publish_time），来自三个快讯源翻页采集 |
| 输出 | 干净的 `News` 列表，每条含：`category` 分类标签、`is_relevant` 相关性、`source_count` 多源计数、`source_urls` 多源链接 |

### 2.3 内部流水线（4 步）

```
原始新闻 ──①相关性过滤──▶ 金融相关子集 ──②语义去重──▶ 去重子集 ──③多源合并──▶ 合并结果 ──④分类落库──▶ 干净新闻
```

下面分步详述。

### 2.4 步骤①：相关性过滤（LLM 批量分类）【解决「金融无关」】

- **做法**：把 20~30 条新闻的「标题 + 摘要」打包进**一个** prompt，让 LLM 一次性输出一个 JSON 数组，每条输出四个字段：
  - `relevant`：`true/false`（是否与金融市场相关）
  - `category`：内容大类（仅 relevant=true 时有效）
  - `market`：市场维度（仅 relevant=true 时有效）
  - `themes`：题材/概念标签数组（0~3 个，仅 relevant=true 时有效）

- **分类标签体系**（三个维度，喂给下游研判 Agent 用）：

  **① 内容大类 `category`**（单选，8 类）：

  | 标签 | 含义 | 示例 |
  |------|------|------|
  | `宏观` | 货币/财政**总量**政策、GDP、CPI、PMI、利率 | "央行开展逆回购""LPR 下调" |
  | `政策` | 产业/监管政策（影响特定行业） | "证监会新规""新能源补贴" |
  | `行业` | 板块、产业链、产能、价格 | "半导体涨价""光伏产能管控" |
  | `公司` | 个股公告、业绩、重组、人事 | "某公司拟回购""业绩预增" |
  | `国际` | 海外市场、地缘、汇率、大宗 | "美联储议息""美元走强" |
  | `资金` | 北向资金、融资融券、基金、ETF/REITs | "北向净流入""ETF 份额" |
  | `市场` | 大盘综述、指数表现、成交量、龙虎榜、市场情绪 | "三大指数集体收涨" |
  | `综合` | **无法归入上述任何一类**（突发事件、跨类别、会议活动） | "国际地热论坛举行" |

  > **重要设计约束**（2026-09-14 修订）：
  > 1. **`行业` 不再是兜底类别**。早期实现的 `_normalize_category()` 在无法识别时默认返回 `行业`，
  >    导致 `行业` 成为垃圾桶（实测抽样 12 条中约 5~6 条不属于行业）。现已改为兜底到 `综合`。
  > 2. **Prompt 必须给 LLM 诚实的出口**：明确要求"若确实无法归入上述任何一类，请选 `综合`，
  >    **不要勉强归类到最接近的类别**"。否则 LLM 被逼硬选，仍会污染最宽的类别。
  > 3. `宏观` 与 `政策` 的边界：**总量政策**（影响全市场）归 `宏观`；**产业/监管政策**（影响特定行业）归 `政策`。
  > 4. 原 `其他` 类别已删除（语义与 `综合` 重复；非金融新闻在 `relevant=false` 阶段即被丢弃）。

  **② 市场维度 `market`**（单选）：`A股` / `美股` / `港股` / `全球` / `无`

  **③ 题材概念 `themes`**（多选，0~3 个自由词）：如 `AI`、`机器人`、`半导体`、`新能源`、`算力` 等，由 LLM 从新闻内容自由提取

- **成本**：1000 条 ≈ 40~50 次调用，约几毛钱/天。
- **兜底**：LLM 判断失败（超时/解析失败）时，默认按 `relevant=false` 丢弃，宁可漏采不脏库。

### 2.5 步骤②：语义去重（向量相似度 + LLM 复核）【解决「跨源重复」】

- **主判据**：对每条金融相关新闻，用 bge-m3 算向量，查 ChromaDB 取 top-1 相似度：
  - `similarity ≥ 0.85` → 判为**重复**（同一事件）
  - `0.70 ≤ similarity < 0.85` → **灰色区间**，交给 LLM 复核（"这两条是不是同一件事"）
  - `< 0.70` → 判为新新闻
- **为什么加 LLM 复核**：0.70~0.85 区间存在「极相似但不同事件」的边界案例（如"某板块连续两天的不同消息"），向量无法分辨，LLM 能。
- **实现要点**：复用现有 ChromaDB 查询（`get_collection().query(query_embeddings=[v], n_results=1)`），相似度 = 1 − cosine distance，无需两两比对。

### 2.6 步骤③：多源佐证合并【你选的第 4 点】

- **原则**：同一事件不删重复，而是**合并成一条主记录，标记「有多少源佐证」**。
- **合并规则**：
  1. 主记录保留**信息最全**的那条（正文最长的作为 content/title 基准）。
  2. `source` 字段记为**主来源**，`url` 记主来源的链接。
  3. 新增字段记录佐证：`source_count`（几个源报道）+ `source_urls`（各源链接）。
- **价值**：多源同时报道 = 可信度高、重要度高。研判 Agent 可以据此加权（如 `source_count ≥ 2` 的新闻视为"强信号"）。
- **触发时机**：**入库时实时合并**。新新闻若命中库内重复，则**更新**已有记录的 `source_count` / `source_urls`，而不是插入新行。

### 2.7 数据模型变更（news 表）

在现有 `News` 模型上**新增 4 个字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `category` | String(50) | 已存在，改为由数据 Agent 填充 |
| `market` | String(20) | 市场维度：A股/美股/港股/全球/无 |
| `themes` | Text（JSON 数组） | 题材概念标签，如 `["AI","半导体"]` |
| `source_count` | Integer, 默认 1 | 报道该事件的源数量（仅展示，不参与加权） |
| `source_urls` | Text（JSON） | `[{"source":"新浪财经","url":"..."}, ...]`，多源链接 |

> 现有 `source` / `url` 字段语义不变，仍表示"主来源"。`source_count` 仅作展示信息，研判时不参与加权（已确认）。

### 2.8 输出契约（给下游的约定）

数据采集 Agent 完成后，保证库中新增/更新的新闻满足：
1. 全部 `relevant=true`（无金融无关内容）。
2. 无跨源重复（每个事件仅一条主记录）。
3. 每条带 `category` 标签。
4. 重复事件正确累加 `source_count`。

---

## 3. ② 分析研判 Agent（Analyst Agent）

### 3.1 职责

基于①产出的干净新闻 + 行情数据，做**情绪打分 + 多维分析 + 生成研判报告**。

### 3.2 输入 / 输出

| | 内容 |
|---|---|
| 输入 | 当天干净新闻（带 category / source_count）+ 主要指数行情（近 30 天）|
| 输出 | 结构化 `MarketReport`（沿用现有结构：sentiment / score / key_drivers / sector_opportunities / risks / reference_news） |

### 3.3 内部流程（沿用现有 graph 逻辑，升级两点）

```
组装行情快照 → 按 category 分维度检索新闻 → 情绪打分 → 多维度分析 → 生成结构化报告
```

相对现有实现，升级两点：
1. **情绪打分改批量**：对当天新闻，LLM 批量打分（多条一次），返回每条的 score(-1~1)，替代现在的逐条打分。
2. **引入 source_count 加权**：研判时把 `source_count` 作为新闻可信度信号，多源佐证的新闻在关键驱动因素中优先引用。

### 3.4 多维度分析框架

| 维度 | 数据来源 | 产出 |
|------|---------|------|
| 宏观面 | `宏观` + `政策` 类新闻 | 政策/流动性判断 |
| 资金面 | `资金` 类新闻 + 行情成交额 | 资金流向判断 |
| 情绪面 | 全部新闻的情绪分 + source_count | 市场情绪分 |
| 行业面 | `行业` + `公司` 类新闻 | 板块机会 |
| 国际面 | `国际` 类新闻 + 美股行情 | 外部风险 |

> 每个维度由 `category` 标签驱动检索，这也是①的分类标签能直接复用到这里的原因。

### 3.5 报告结构（不变）

沿用现有 `MarketReport` 的 JSON 结构，不再新增字段，保持前端兼容。

---

## 4. ③ 问答 Agent（QA Agent）

### 4.1 职责

交互式问答：理解用户问题 → 检索 + 工具调用 → 综合回答。

### 4.2 输入 / 输出

| | 内容 |
|---|---|
| 输入 | 用户 query + 对话历史 |
| 输出 | 文本回答 |

### 4.3 内部流程（基本不变）

沿用现有 `create_agent`（ReAct/tool-calling），工具集小幅增强：

| 工具 | 说明 | 变化 |
|------|------|------|
| `search_news` | RAG 语义检索新闻 | 不变，但检索结果现在只含干净新闻 |
| `get_market_overview` | 查行情 | 不变 |
| `get_recent_news` | 按关键词/分类查新闻 | **新增 category 筛选参数** |
| `get_report_history` | 查历史报告 | 不变 |

### 4.4 与主流程的关系

QA Agent 是**独立服务**，不参与每日定时主流程，仅在用户提问时触发。它读取①②沉淀到库里的数据。

---

## 5. Agent 间协作与时序

```
【每日定时任务】
17:30  行情采集（现有）
18:00  数据采集 Agent：采集 → 过滤 → 去重 → 合并 → 入库 + 索引
18:15  分析研判 Agent：情绪打分 → 多维分析 → 生成报告 → 入库
【按需】
用户提问 → 问答 Agent → 检索 + 工具 → 回答
```

- 数据采集 Agent 是分析研判 Agent 的**上游**，通过数据库交接。
- 若①产出的新闻为空（比如周末无快讯），②自动跳过，沿用最近一份报告。

---

## 6. 技术实现要点（LangChain 1.x）

| Agent | 实现形态 | 说明 |
|-------|---------|------|
| ① 数据采集 | **Pipeline + LLM 工具** | 不是自主循环 agent；采集/去重是传统逻辑，只有「相关性判断」「灰色复核」是 LLM 调用。用 LangChain 的 `@tool` 封装 LLM 分类函数即可 |
| ② 分析研判 | **LangGraph 固定流程** | 沿用现有 graph 逻辑，线性、可复现 |
| ③ 问答 | **create_agent** | 沿用现有 ReAct/tool-calling，改动最小 |

> 说明：这里三个 Agent 不是 LangGraph 的「supervisor 协作图」，而是**职责分明的三个模块**，通过数据库解耦。等未来需要跨 Agent 动态调度时，再引入 supervisor。

---

## 7. 成本估算（每日）

| 环节 | LLM 调用次数（约） | 估算 |
|------|------------------|------|
| 相关性过滤（批量） | 40~50 次 | 几毛钱 |
| 灰色区间复核 | 少量（<10 次） | 忽略 |
| 情绪打分（批量） | 40~50 次 | 几毛钱 |
| 研判报告生成 | 1 次 | 忽略 |
| **合计** | **约 100 次/天** | **< 1 元/天** |

---

## 8. 已确认的决策（2026-09-06）

1. **分类标签体系**：三个维度 —— 内容大类（7 类）+ 市场维度（A股/美股/港股/全球）+ 题材概念（自由词）。
2. **去重阈值**：0.85（重复）/ 0.70~0.85（LLM 复核），先按此实现，后续可调。
3. **非金融新闻**：直接丢弃，不留档。
4. **source_count**：仅作展示信息，研判时不加权。
5. **数据采集 Agent 形态**：混合 Pipeline（LLM 判断 + 向量计算），非纯 LLM 自主循环。
