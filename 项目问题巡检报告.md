# 项目问题巡检报告

检查范围：后端 `app/`、Streamlit 前端 `frontend/`、运维/契约脚本 `scripts/`、运行配置及相关设计约束。本文只记录问题与修复方向，不修改应用代码。

## 检查基线

- 63 个 Python 文件通过 AST 语法解析。
- `python -m compileall -q app frontend scripts` 通过。
- `import app.main` 通过。
- UTF-8 输出下，现有离线契约脚本通过：采集 41/41、评估 32/32、时效 22/22、健康 26/26、入库 91/91、LLM 15/15、调度 32/32。
- 工作区已有未提交改动未覆盖、未回滚；本次只新增本报告。

严重度说明：高 = 可能造成未授权操作、重复任务/数据错误或用户请求直接 5xx；中 = 可导致明显数据质量、可用性或运维误判；低 = 边界鲁棒性或维护性问题。

## 高严重度

### H1. 监听所有网卡且无请求来源限制（暴露时风险高）

- 位置：`app/main.py:103`、`app/api/routes_news.py:108-112`、`app/api/routes_market.py:39-43`、`app/api/routes_reports.py:49-69`、`app/api/routes_agent.py:25-28`。
- 问题：启动示例监听 `0.0.0.0`（`README.md:56`）；采集、生成报告、评估（可调用 LLM）和 Agent 问答端点没有鉴权，CORS 使用 `allow_origins=["*"]`（`app/main.py:92-97`）。设计文档将多用户权限体系列为 v1 非目标，因此这不是脱离部署条件的必然缺陷。
- 触发条件/影响：仅当 8000 端口可被不受信任的局域网、互联网或反向代理客户端访问时，调用方才能触发外部 API 消费、写数据库或长时间任务，带来费用、数据污染或拒绝服务风险。
- 修复方向：默认只绑定 loopback；部署时用防火墙/安全组限制来源，反向代理不得无保护转发。若确有跨信任边界访问需求，再决策是否补认证，并同步收紧 CORS 来源。
- 把握：暴露面与无鉴权为确认；实际可达范围及风险等级取决于部署网络，未验证。

### H2. 日报“每日唯一”约束没有真正按天生效

- 位置：`app/models/report.py:17`、`app/agent/graph.py:581-617`、`app/services/report_service.py:127-138`。
- 问题：数据库唯一约束作用于完整 `datetime`，实时生成将 `datetime.now()`（含微秒）写入 `MarketReport.date`。同一天多次触发不会违反约束；代码只能在回测时事后按日期去重。
- 触发条件/影响：定时任务、手动接口或多 worker 同时生成日报时，同日可落多份报告；“today”接口取最后一份，早先结果被隐藏，评估/审计也依赖补救式去重。
- 修复方向：把业务日期与生成时刻拆开，或将日报唯一键改为 `(report_type, report_day)`；写入前以交易日/报告日做幂等 upsert，并为并发冲突补充事务回滚和重试。迁移前清理历史同日重复记录并保留审计关系。
- 把握：确认（模型约束、写入值和回测注释均直接证明）。

### H3. 多进程/热重载下调度器和采集锁无法保证单实例

- 位置：`app/main.py:74-83`、`app/collectors/scheduler.py:202-231`、`app/services/news_service.py:182-195`、`README.md:56`。
- 问题：每个 Uvicorn worker 都会执行 lifespan 并启动一个 `BackgroundScheduler`；`_COLLECT_LOCK` 只是进程内 `threading.Lock`。项目示例使用 `--reload`，热重载会重复启动生命周期，但通常旧进程会先退出，单凭 reload 不足以断定任务同时重复执行。
- 触发条件/影响：以多个 worker 或多个服务进程运行时，各自会启动一份调度器并可同时采集，造成重复 LLM 调用、SQLite 写竞争和水位线竞态；同一进程内的锁无法互斥其他进程。
- 修复方向：将调度器移出 Web worker，单独运行；或使用分布式/文件/数据库锁并只选一个 scheduler leader。开发热重载时提供关闭调度器的配置，生产部署明确禁止每 worker 启动调度器。
- 把握：确认（锁和 scheduler 的进程内生命周期可直接确认；重复运行影响属于确定性推论）。

## 中严重度

### M1. 前端不能处理历史/异常格式的报告内容

- 位置：`app/services/report_service.py:69-83`、`frontend/streamlit_app.py:58-64`、`frontend/streamlit_app.py:197-208`。
- 问题：`report_to_dict` 遇到非 JSON 的旧报告内容时返回字符串；前端随后无条件调用 `c.get(...)`。模型注释也允许内容是 Markdown，因此旧数据或手工记录会走该分支。
- 触发条件/影响：打开含旧 Markdown 内容的日报/周报时，Streamlit 页面抛 `AttributeError`，整页无法渲染。
- 修复方向：API 固定返回结构化对象并增加 `content_format`；或前端先判断 `isinstance(c, dict)`，对字符串显示原文/降级提示。
- 把握：确认（用内存 SQLite 插入 `legacy markdown` 已观察到 API 返回 `str`，前端调用链确定会访问 `.get`）。

### M2. 前端对 `score=None` 的格式化会抛异常

- 位置：`frontend/streamlit_app.py:103-106`、`frontend/streamlit_app.py:204-208`。
- 问题：`data.get("score", 0)` 只在键缺失时使用 0；键存在且值为 `None` 时仍执行 `f"{None:+.2f}"`。
- 触发条件/影响：历史报告、聚合失败或迁移数据中 `score` 为 NULL 时，日报或周报页面渲染失败。
- 修复方向：统一使用 `score = data.get("score"); score_text = ... if isinstance(score, (int,float)) else "—"`，并在 API 层明确数值字段的空值契约。
- 把握：确认（Python 格式化行为和模型 nullable 定义直接可验证）。

### M3. 周报定调绕过可配置的综合分阈值

- 位置：`app/agent/weekly.py:180-185`、`app/config.py:123-126`。
- 问题：日报汇总和回测使用 `settings.score_neutral_band`，周报却硬编码 `0.15`。配置调整后，日报/回测与周报对同一分数可能给出不同方向。
- 触发条件/影响：修改 `.env` 或配置对象中的中性带后，周报 `sentiment` 仍按旧阈值计算，造成跨页面和统计口径不一致。
- 修复方向：周报使用同一配置字段（并复用统一的定调函数），增加阈值变更的边界测试。
- 把握：确认（同一业务口径存在明确的硬编码分叉）。

### M4. 调度时间配置没有统一校验，非法值会阻止启动

- 位置：`app/collectors/scheduler.py:141-143`、`app/collectors/scheduler.py:207-230`、`app/config.py:209-235`。
- 问题：`_parse_hhmm` 只做字符串拆分和整数转换；`report_time`、`market_collect_time`、`sector_collect_time`、`weekly_report_time` 没有 Pydantic 范围校验。
- 触发条件/影响：配置为缺少冒号、非数字或 `25:00`/`18:60` 时，`start_scheduler()` 抛异常，FastAPI lifespan 启动失败。
- 修复方向：在 Settings 中用字段校验限制 `HH:MM` 和 `0<=HH<24, 0<=MM<60`；无效配置在启动前给出明确错误，或按已记录的安全默认值回退并告警。为四个时间字段补边界测试。
- 把握：确认（异常路径直接可见；未实际改配置启动）。

### M5. 行情手动采集与定时采集没有互斥/幂等冲突处理

- 位置：`app/api/routes_market.py:39-43`、`app/collectors/scheduler.py:25-38`、`app/services/market_service.py:10-23`。
- 问题：`save_market_data` 采用“先查后插”，没有事务级 upsert，也没有捕获 `UniqueConstraint(symbol,date)` 冲突；行情端点和 scheduler 没有类似新闻采集的互斥锁。
- 触发条件/影响：两个请求或多进程同时写同一指数日期时，两边都查不到记录，后提交者可能收到 500，事务状态还需显式 rollback 才能复用会话。
- 修复方向：使用数据库原生 upsert/`ON CONFLICT DO NOTHING`，或捕获 `IntegrityError` 后 rollback 并重新读取；将手动与定时采集纳入同一跨进程锁。
- 把握：推断但高把握（竞态由代码结构确定，需并发测试在目标 SQLite 配置上复现）。

### M6. 无法解析的行情日期被静默改成当前时间

- 位置：`app/collectors/market_collector.py:37-49`。
- 问题：源数据日期为空或解析失败时返回 `datetime.now()`，而不是拒绝该行或标记脏数据。
- 触发条件/影响：上游字段改名、空值或格式变化会把历史行情伪装成今天的数据，可能与真实今日记录发生唯一键冲突，也会污染回测和“最新行情”展示。
- 修复方向：解析失败返回 `None` 并丢弃/记录该行，或让采集结果显式携带 `invalid_rows`；禁止用当前时间作为数据源日期兜底。
- 把握：确认（兜底行为直接可见；具体上游异常需外部数据触发）。

### M7. 列表查询参数没有边界校验，负数可改变数据库分页语义

- 位置：`app/api/routes_news.py:75-104`、`app/api/routes_market.py:13-21`、`app/api/routes_reports.py:38-46`；Agent 工具 `app/agent/tools.py:51-60`、`app/agent/tools.py:101-111`。
- 问题：`limit`、`days` 等参数为裸 `int`。SQLite 的 `LIMIT -1` 表示不限制行数；极大 limit 会把大量数据一次性序列化，Agent 工具也可传入任意 limit。
- 触发条件/影响：恶意或错误请求可能导致大查询、内存/响应耗尽；`days=0` 被静默视为未传，负数产生反直觉的未来起始日期。
- 修复方向：使用 `Query(ge=1, le=<上限>)` 或 Pydantic 参数模型；明确 `days>=1`，校验 `start<=end`，对 Agent 工具内部再做一次硬上限。
- 把握：确认（参数无约束；SQLite 负 LIMIT 语义已知）。

### M8. 健康接口只返回静态 `ok`，不能表示核心依赖就绪

- 位置：`app/main.py:103-104`。
- 问题：`/api/v1/health` 不检查数据库连接、迁移状态、scheduler、BM25、交易日历或采集水位线；服务进程存活但核心功能失效时仍返回 200 `{"status":"ok"}`。评估模块中的 `check_retrieval_health` 是独立评估项，不是 readiness API。
- 触发条件/影响：负载均衡和监控会把不可用实例视为健康，故障发现延迟；尤其启动预热在后台线程失败时，健康端点没有反映。
- 修复方向：拆分 liveness/readiness；liveness 只表示进程存活，readiness 检查数据库和关键依赖，并以结构化状态/HTTP 503 暴露降级原因。
- 把握：确认（端点实现仅返回常量）。

## 低严重度

### L1. 灰区判重用子串判断布尔值

- 位置：`app/agent/data_agent.py:656-680`。
- 问题：`return "true" in (resp.content or "").lower()` 会把 `"not true"`、包含说明文字的非标准响应误判为重复。
- 触发条件/影响：模型未严格遵守“只输出 true/false”时，真实新新闻可能被合并，且错误不可追溯。
- 修复方向：先规范化去代码围栏和空白，只接受精确 `true`/`false`；其余返回 `None`，沿用当前 fail-open 并记录原始分类为解析失败。
- 把握：确认（字符串判定逻辑直接成立，异常输出是否出现需外部模型触发）。

### L2. Agent 行情概览每次都加载全量历史数据

- 位置：`app/agent/tools.py:32-45`。
- 问题：`get_market_overview` 直接读取全部行情行，再在 Python 内存中为每个指数保留最新记录。
- 触发条件/影响：行情历史长期增长后，每次相关问答都要加载全表，增加数据库读负载、延迟和内存占用。
- 修复方向：在 SQL 中按 symbol 取最新记录，或限定有业务意义的时间窗/最大行数；避免先物化全量历史数据。
- 把握：推断（性能影响取决于数据量，但代码路径无上限）。

### L3. `extract_json` 使用贪婪正则提取 JSON，可能选错片段

- 位置：`app/agent/experts.py:19-34`、`app/agent/data_agent.py:149-163`。
- 问题：在完整 JSON 解析失败后使用 `\{.*\}` / `\[.*\]` 贪婪匹配；当响应含多个对象/数组或前后有示例时，可能截取跨段无效文本并放弃本可解析的后一个对象。
- 触发条件/影响：LLM 输出带解释、多个候选 JSON 或嵌套结构时，分类/报告进入失败降级，增加重复采集或空报告概率。
- 修复方向：优先使用结构化输出/JSON mode；否则采用括号计数解析或非贪婪候选扫描，并为多 JSON 文本补测试。
- 把握：推断（正则行为确定，是否被实际模型输出触发未验证）。

## 未列为问题的检查项

- 未发现 Python 语法错误或导入错误。
- 空 Chroma 集合查询经实测返回空结果，未列为“空库崩溃”。
- 现有契约脚本在 `PYTHONUTF8=1` 下全部通过；此前 Windows GBK 输出导致的 `UnicodeEncodeError` 属于巡检脚本输出环境问题，不等同于业务逻辑失败。

## 修复优先级建议

1. 先处理 H1/H2/H3：认证与暴露面、调度单实例、日报幂等，否则会持续产生外部成本和数据污染。
2. 再处理 M1-M8：先修前端空值/旧数据兼容和配置校验，再补采集写入的并发 upsert、日期拒绝策略与参数限流。
3. 最后处理 L1-L3，并为每一项补最小回归测试；所有修复后重新运行现有契约脚本和启动/接口冒烟测试。

---

## 代码 Agent 的处置记录（2026-09-25）

> **本文档的 14 条已全部处置**（13 条修、1 条 L3 缓办见下），另含核实过程中发现的 4 条漏报。
> 提交：`a4e3de8` ← `f7dc9a2` ← `7a98dd6` ← `be105ef` ← `495e6e5`（均已推送）。
>
> **每条都先独立核实再改** —— 核实纠正了本报告 **5 处**，那些"结论"不要照用。

### 1. 核实纠正的 5 处（改前必读）

| 本报告的说法 | 核实结果 |
|---|---|
| **M6** 后果是「唯一键冲突 → 500」 | ❌ **不成立**。唯一键建在**完整 datetime** 上，`now()` 带微秒而真实行情是当日 00:00 → **不冲突、不报错，两行并存**。真实危害更隐蔽：`get_market_overview` 的 `setdefault` 与回测的 dict 覆盖都是**按时间戳取最晚**，于是**坏行反而成了「最新行情」**，静默盖掉真实数据 |
| **M2** 修法 `data.get("score")` | ❌ **无效修法** —— 带默认值的 `.get` 在「键存在、值为 None」时**照样返回 None**。正确写法要 `or 0`，而本项目更该显示 `—`（见下） |
| **M1** 触发条件是「非 JSON → str」 | ⚠️ **漏了一支**：**合法 JSON 但顶层是数组** → `list`，同样没有 `.get`，一样崩 |
| **L2** 暗示「没有 `(symbol,date)` 索引」 | ❌ **索引是有的**（`UniqueConstraint` 带来的复合唯一索引）。「每次全量加载」属实 |
| **L3** 「可能选错片段」 | ⚠️ **高估**：贪婪匹配跨到末括号，几乎必然解析失败 → 后果是「提取不到 → 重试/兜底」，**不存在静默拿错数据**的路径 |
| **H3** 定为「高严重度」 | ⚠️ **对默认部署高估**：已读 uvicorn 源码确认 reloader 父进程**不跑 lifespan**、重启是先停旧再起新 → `--reload` 单 worker 不触发。只在 `--workers N` 下成立 |

### 2. 报告漏报、核实过程发现的 4 条

1. **`datetime.now()` 兜底不止行情有** —— 新闻侧还有 **3 处**（`news_service.py:126`、`data_agent.py:737`、`data_agent.py:625`），其中 `_nearest_existing` 那处**直接影响去重的 24h 时间窗**。⚠️ **本条未改**（见 §4）。
2. **行情表唯一键同样建在时间戳上** —— 与报告 H2 批评日报的是**同一个病**，是 M5/M6 的共同根因。
3. `get_market_overview` + `compute_backtest` 对同日多行都是 **last-wins** → 坏行静默流向问答与回测。
4. **`limit=-1` 实测绕过上限**（返回全表 9543 行）；SQLAlchemy 2.0 会原样绑定 `-1` 下发。

### 3. 逐条处置

| 编号 | 处置 | 提交 |
|:--:|---|:--:|
| **H1** | README 默认绑定 `0.0.0.0` → `127.0.0.1`，并写清部署约束（反代鉴权 / 限制来源 / 收紧 CORS / **别用 `--workers`**）。**不加鉴权** —— `DESIGN.md` §1.3 明列 v1 非目标，这是部署问题 | `7a98dd6` |
| **H2** | 新增 `report_day`（业务日）+ `(report_type, report_day)` 唯一索引；`_migrate_report_day()` 补列→回填→补 NULL 的 `report_type`→**留痕后**清理重复→**最后**建索引；日报改**同日原地覆盖** | `be105ef` |
| **H3** | 跨进程文件锁（`msvcrt.locking` / `fcntl.flock`），拿不到锁的进程只提供 API 并在日志说清；`start_scheduler()` 改为返回布尔 | `495e6e5` |
| **M1** | `report_to_dict` 一处收口：`content` 非 dict 一律包成 `{"raw": ...}`（**两支都覆盖**，含报告漏掉的数组支） | `f7dc9a2` |
| **M2** | 新增 `_fmt_score`：非数值显示 **`—`**，**不兜成 0** —— 0 是「中性」这个真实结论，None 是「拿不到」，兜成 `+0.00` 等于凭空造结论（与本页 `divergence` 那行同源） | `f7dc9a2` |
| **M3** | 周报定调改用 `settings.score_neutral_band`（它是全项目**第 4 个**用中性带的地方，而 config 注释写着「三处共用，不许各写各的」） | `f7dc9a2` |
| **M4** | 四个调度时刻加 `HH:MM` 校验。**选「回退 + 大声告警」而非抛错** —— 与本项目对 `NEWS_INTERVAL_MINUTES` 的既有立场一致（「这比漂移更糟，是**启动失败**」） | `7a98dd6` |
| **M5** | `save_market_data` 改原生 `ON CONFLICT DO NOTHING`（查与插之间不再有窗口，也就无需 rollback 补丁）；另加行情侧进程锁 `_MARKET_LOCK` | `a4e3de8` |
| **M6** | `_to_date` 失败返回 `None`，采集循环丢弃该行并告警。⚠️ 实现时发现 `pd.to_datetime('')`/`nan` **返回 `NaT` 而非抛错**，只判 `is None` 拦不住 | `a4e3de8` |
| **M7** | news/market `limit` `[1,1000]`、reports `[1,200]`、`days` `[1,30]`；Agent 工具再夹一次硬上限（参数由 **LLM** 生成，不可信） | `f7dc9a2` |
| **M8** | 拆存活/就绪两个探针。**只有数据库失败给 503** —— BM25 空只是检索降级、调度器停只是不再自动采集，算 503 会让负载均衡在**服务其实能用**时把它摘掉 | `7a98dd6` |
| **L1** | 灰区判重改**精确匹配**（`_normalize_verdict` 三态）。原写法把 `not true`（**语义相反**）、`untrue`、`true. 因为…` 全判成「重复」，而误合并**不可逆** | `f7dc9a2` |
| **L2** | `get_market_overview` 改 SQL 取每指数最新。**实测与旧实现逐字段等价**（7 条），不是靠推理 | `f7dc9a2` |
| **L3** | ⏸ **缓办** —— 核实认为严重度被高估（不存在静默拿错数据的路径），且 `extract_json` 有两份（`experts.py` / `data_agent.py`），改动面与收益不成比例。**这是我的判断，不是漏掉**；若你要做，说一声 | — |

### 4. 我**没有**照报告做的地方（附理由）

1. **M4 选了「回退」而不是「抛错」** —— 见上表。
2. **M6 新闻侧的 3 处 `datetime.now()` 兜底未改** —— 报告没提这 3 处，且改法有分叉（丢弃 = 可能丢新闻；保留 = 伪造时间）。行情侧的改法（丢弃 + 告警）在那边代价更大，**需要你拍板**。
3. **L3 缓办** —— 见上表。
4. **H1 不加鉴权** —— 尊重设计文档的 v1 非目标。

### 5. 实施过程中**我自己犯的错**（都由测试/变异抓出，如实记录）

| # | 错 | 怎么被抓出来的 |
|:--:|---|---|
| 1 | `_to_date` 第一版只判 `is None`，漏了 `NaT` | 测试用例（`''` / `nan` / `pd.NaT`） |
| 2 | `db.execute()` 对 ORM 实体 + 参数列表返回 `IteratorResult`，**没有 `rowcount`** | 直接 `AttributeError` |
| 3 | `db.py` 里 `text` 是函数内局部导入，模块级函数拿不到 → `NameError` | 副本排练 |
| 4 | `upsert_daily_report` 的 `fields` **能覆盖身份键** —— 传 `report_type=None` 把已有日报改成 NULL，而 SQLite 认为 NULL 互不相等，「一天一份」当场失效 | 契约脚本（已加护栏） |
| 5 | **一条断言在为错误的理由通过**：「直接插同日重复被拒」用的是**同一个 `date`**，被 `date` 上的旧唯一索引拒了，与 `(report_type, report_day)` 无关 | **变异测试**（摘掉模型里的 `Index` 却不 FAIL） |
| 6 | `check_api_contracts.py` 读 `Query` 的 `.ge` 属性 → 4 条**假 FAIL**（FastAPI 0.141 + Pydantic 2.13 把约束放进 `metadata`） | 打印被检查的原值 |
| 7 | `check_market_contracts.py` 的源码守卫被 **docstring 里解释性的 `datetime.now()`** 判成假 FAIL | 同上（改用 AST 判定） |
| 8 | 两次变异导致 **Traceback 而非 FAIL 行**（断言没接住被测代码抛的异常） | 变异测试（已包 `_safe`） |

> 第 5–8 条是同一个毛病：**检查工具自己没在检查那件事**。这已是本项目第 4~6 次。

### 6. 验证（三态）

**✅ 通过（实跑看到结果）**

| 项 | 证据 |
|---|---|
| 契约脚本 | 9 个全绿：schedule **45**、collector 41、health 26、llm 15、freshness 22、ingest **101**、eval 32（**0 跳过**）、api **40**、market **15** |
| 新增脚本 | `check_api_contracts.py`（40）、`check_market_contracts.py`（15）—— 行情链路此前**一个契约脚本都没有** |
| 变异测试 | 本批 **7/7 检出**（时间校验、NaT 判定、裸插、content 收口、limit 夹取、子串判重、模型索引、身份键护栏、锁判断） |
| **真库迁移** | 先备份 `data/app.db.bak-before-report-day` + **先在副本排练**；真库 13 行 → 9 行，4 条删除**全部留痕**，`report_day` 零 NULL，索引已建；**第二次 `init_db` 清理 0 行（幂等）** |
| **跨进程锁** | A 进程持锁 → B 进程 `start_scheduler()` 返回 **False 且未注册 job**；A 退出后锁**由内核自动释放** |
| **真实双进程部署** | 第二个后端日志出现「**另一个进程已持有调度器锁 —— 本进程不启动调度器**」；其就绪探针如实报 `degraded` + `scheduler_running: false` |
| 启动冒烟 | 后端已用**新的默认绑定** `--host 127.0.0.1` 重启；`/api/v1/health` → ok；`/api/v1/health/ready` → `{"status":"ok","checks":{"database":true,"bm25_ready":true,"bm25_size":5981,"scheduler_running":true}}` |

**⚠️ 尚未验证**

| # | 项 | 卡在哪 |
|:--:|---|---|
| 1 | 行情侧「上游字段改名」的真实触发 | M6 是**潜伏缺陷** —— 实测库里 543 条行情**零坏行**，这条 bug 从未真正触发过 |
| 2 | 多 worker 下第二个 worker 的真实重复执行 | 只验到「第二进程不启动调度器」；没跑过 `--workers 2` 的全链路 |
| 3 | 新闻侧 3 处 `datetime.now()` 兜底的实际影响面 | 未改（见 §4 第 2 条） |

### 7. 需要你拍板的两处

| # | 事项 | 我的建议 |
|:--:|---|---|
| 1 | **M6 新闻侧的 3 处 `datetime.now()` 兜底**改不改 | 我倾向**只加告警、不改行为**（丢弃新闻的代价比时间不准大）；但 `_nearest_existing` 那处会污染 24h 去重窗，可以单独收紧 |
| 2 | **L3** 是否实施（两份 `extract_json` 一起改） | 建议**暂缓** —— 后果是「提取失败→重试」，而重试路径本身是对的 |

---

*本节由代码 Agent 撰写，处置已落代码；未采纳报告建议之处均已附理由。*
